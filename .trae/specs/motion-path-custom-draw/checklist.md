# Checklist

- [ ] UI 开关正常工作且位置符合预期（动画控制区）。
- [ ] 原生 Motion Path 面板与相关 Operator 已移除。
- [ ] `build_position_cache` 正确生成完整的世界空间路径数据（每一帧）。
- [ ] 自定义路径线 GPU 绘制逻辑（Batch 生成与 Draw）正确实现。
- [ ] 路径线显示颜色正确，且能区分选中/未选中状态（如果实现）。
- [ ] 拖拽手柄时路径线能实时更新（无需额外点击）。
- [ ] 插件加载/重载后路径线能正确初始化显示。
- [ ] 无报错（Console 无异常输出）。
- [ ] 性能测试：简单场景下帧率无明显下降。
